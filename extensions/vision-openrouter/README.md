# OpenRouter Vision Extension

Registers a custom `openrouter_vision` tool for pi-coding-agent that downloads Telegram inline images and sends them to a low-cost OpenRouter multimodal model (`openai/gpt-4o-mini` by default). Requires `TELEGRAM_BOT_TOKEN` and `OPENROUTER_API_KEY` environment variables.
