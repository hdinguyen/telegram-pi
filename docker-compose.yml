services:
  telebot:
    build: .
    image: telebot
    container_name: telebot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./.pi/skills:/app/.pi/skills
